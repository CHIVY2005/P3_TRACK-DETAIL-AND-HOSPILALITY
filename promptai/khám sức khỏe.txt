Role: You are an Autonomous QA, DevOps, and Backend SDET Engineer.

Context: We have completed the architecture for the "Guardian Pricing Platform" backend (FastAPI, PostgreSQL with pgvector, SQLAlchemy, and an Apify scraping service). We are now in the End-to-End (E2E) Execution and Testing phase.

Task: Autonomously execute the entire testing pipeline, monitor the system logs, self-heal any code-level bugs, and generate reports.

Step-by-Step Execution Pipeline:
Step 1: Spin up Infrastructure. Run docker compose up -d in the root directory. Ensure the PostgreSQL container is fully healthy and accepting connections on port 5432.
Step 2: Seed the Database. Run the database seed script (e.g., python backend/seed_db.py). Monitor the terminal output.
Step 3: Boot the Backend. Start the FastAPI server (e.g., uvicorn app.main:app --reload from the backend directory).
Step 4: Execute API Test. Trigger a test request to POST /api/sync-price/8931111111111 (or another barcode seeded in Step 2) using curl or a Python test script.

Autonomous Self-Healing & Debugging Rules (CRITICAL):

You MUST read and analyze the terminal/server logs at every step.

Actionable Errors (Fix automatically): If you encounter code-level errors (e.g., ImportError, typo, missing SQLAlchemy column, wrong variable name), you are authorized to fix the Python code immediately and retry the step.

Blocking Errors (Require Human Intervention): If you encounter complex/external errors (e.g., missing APIFY_API_TOKEN, Apify out of credits, Docker port conflicts that you can't kill, or persistent 500 errors after 3 failed self-heal attempts), you MUST STOP execution. Do not guess API keys.

Deliverables:

If Blocked: Create a file named intervention_required.md detailing: The exact error log, what you attempted to do, and explicitly what the human needs to provide/fix (e.g., "Please add APIFY_API_TOKEN to .env").

If Successful: Create a file named test_execution_report.md detailing:

The timestamp of the test.

Steps executed and their status.

Any self-healing actions you took during the process (bugs you fixed).

The final JSON payload returned by the /api/sync-price endpoint to prove the E2E pipeline works.